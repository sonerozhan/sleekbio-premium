<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SleekBio - Premium Streamer & Gamer Template</title>
    <!-- Google Fonts & FontAwesome Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --bg-gradient: linear-gradient(135deg, #0f0c20 0%, #06040a 100%);
            --panel-bg: rgba(255, 255, 255, 0.03);
            --panel-border: rgba(255, 255, 255, 0.08);
            --accent-neon: #00f0ff; /* Cyber Cyan */
            --accent-purple: #7000ff; /* Neon Purple */
            --text-main: #ffffff;
            --text-sub: #8a859e;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: var(--bg-gradient);
            color: var(--text-main);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
            overflow-x: hidden;
        }

        /* CARD CONTAINER */
        .profile-card {
            background: var(--panel-bg);
            border: 1px solid var(--panel-border);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            width: 100%;
            max-width: 480px;
            border-radius: 32px;
            padding: 40px 30px;
            text-align: center;
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.1);
            position: relative;
        }

        /* AVATAR GLOW EFFECT */
        .avatar-container {
            position: relative;
            width: 120px;
            height: 120px;
            margin: 0 auto 24px auto;
        }

        .avatar-img {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 2px solid rgba(255, 255, 255, 0.2);
            position: relative;
            z-index: 2;
        }

        .avatar-glow {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(45deg, var(--accent-neon), var(--accent-purple));
            border-radius: 50%;
            filter: blur(15px);
            opacity: 0.6;
            z-index: 1;
            animation: floatGlow 6s infinite alternate;
        }

        @keyframes floatGlow {
            0% { transform: scale(0.95); opacity: 0.5; }
            100% { transform: scale(1.05); opacity: 0.8; }
        }

        .profile-card h1 {
            font-size: 1.75rem;
            font-weight: 700;
            letter-spacing: -0.5px;
            margin-bottom: 8px;
            background: linear-gradient(to right, #fff, #b9b6cd);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .profile-card .tag {
            font-size: 0.85rem;
            color: var(--accent-neon);
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 2px;
            margin-bottom: 16px;
            display: inline-block;
        }

        .profile-card .bio {
            color: var(--text-sub);
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 32px;
        }

        /* PREMIUM LINKS */
        .links-wrapper {
            display: flex;
            flex-direction: column;
            gap: 16px;
        }

        .premium-link {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid var(--panel-border);
            padding: 18px 24px;
            border-radius: 16px;
            text-decoration: none;
            color: var(--text-main);
            font-weight: 500;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }

        .link-content {
            display: flex;
            align-items: center;
            gap: 16px;
            z-index: 2;
        }

        .link-content i {
            font-size: 1.35rem;
            width: 24px;
            text-align: center;
        }

        .premium-link .arrow-icon {
            font-size: 0.9rem;
            color: var(--text-sub);
            transition: transform 0.3s ease;
            z-index: 2;
        }

        .premium-link:hover {
            transform: translateY(-2px);
            border-color: rgba(255, 255, 255, 0.2);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            background: rgba(255, 255, 255, 0.05);
        }

        .premium-link:hover .arrow-icon {
            transform: translateX(4px);
            color: var(--text-main);
        }

        /* HIGHLIGHT LINK (LIVE NOW) */
        .premium-link.highlight {
            border-color: rgba(0, 240, 255, 0.3);
        }
        .premium-link.highlight:hover {
            box-shadow: 0 0 20px rgba(0, 240, 255, 0.15);
            border-color: var(--accent-neon);
        }

        /* FOOTER BRANDING */
        .footer-brand {
            margin-top: 36px;
            font-size: 0.8rem;
            color: var(--text-sub);
            letter-spacing: 0.5px;
        }

        .footer-brand a {
            color: var(--text-main);
            text-decoration: none;
            font-weight: 600;
        }
    </style>
</head>
<body>

    <div class="profile-card">
        <!-- AVATAR SECTON -->
        <div class="avatar-container">
            <div class="avatar-glow"></div>
            <img src="https://images.unsplash.com/photo-1566492031773-4f4e44671857?q=80&w=500" alt="Creator Avatar" class="avatar-img">
        </div>

        <!-- PROFILE DETAILS -->
        <h1>CreatorName</h1>
        <span class="tag">Live Streamer</span>
        <p class="bio">Welcome to my hub! Catch my live streams, grab my exclusive merch, or join our community Discord below.</p>

        <!-- LINKS -->
        <div class="links-wrapper">
            <a href="#" target="_blank" class="premium-link highlight">
                <div class="link-content">
                    <i class="fa-solid fa-play" style="color: #53fc18;"></i>
                    <span>Watch Me Live on Kick</span>
                </div>
                <i class="fa-solid fa-chevron-right arrow-icon"></i>
            </a>

            <a href="#" target="_blank" class="premium-link">
                <div class="link-content">
                    <i class="fa-brands fa-discord" style="color: #5865F2;"></i>
                    <span>Join the Community Discord</span>
                </div>
                <i class="fa-solid fa-chevron-right arrow-icon"></i>
            </a>

            <a href="#" target="_blank" class="premium-link">
                <div class="link-content">
                    <i class="fa-brands fa-instagram" style="color: #E1306C;"></i>
                    <span>Follow my Instagram</span>
                </div>
                <i class="fa-solid fa-chevron-right arrow-icon"></i>
            </a>

            <a href="#" target="_blank" class="premium-link">
                <div class="link-content">
                    <i class="fa-solid fa-heart" style="color: #ff007f;"></i>
                    <span>Support / Tip Jar</span>
                </div>
                <i class="fa-solid fa-chevron-right arrow-icon"></i>
            </a>
        </div>

        <!-- FOOTER BRANDING -->
        <div class="footer-brand">
            Template by <a href="#" target="_blank">YourBrand</a>
        </div>
    </div>

</body>
</html>